-- -----------------------------------------------------
-- Schema foody-app
-- -----------------------------------------------------
DROP SCHEMA IF EXISTS `foody-app`;
CREATE SCHEMA `foody-app`;
USE `foody-app` ;