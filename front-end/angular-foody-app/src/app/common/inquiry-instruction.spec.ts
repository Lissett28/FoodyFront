import { InquiryInstruction } from './inquiry-instruction';

describe('InquiryInstruction', () => {
  it('should create an instance', () => {
    expect(new InquiryInstruction()).toBeTruthy();
  });
});
