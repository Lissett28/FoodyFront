import { RegisterInstructions } from './register-instructions';

describe('RegisterInstructions', () => {
  it('should create an instance', () => {
    expect(new RegisterInstructions()).toBeTruthy();
  });
});
